# Aegis-CPP

A lightweight Access Control List (ACL) engine for C++ applications and game engines.

## Build

```bash
mkdir build
cd build
cmake ..
cmake --build .
```

## Usage

See `src/main.cpp` for a basic example of registering users, roles, and permissions.
